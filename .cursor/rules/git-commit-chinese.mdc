---
description: 托管项目与本仓统一的中文 Conventional Commits 提交规范（生成 commit message 时必遵）
alwaysApply: true
---

# Git 提交信息规范

> 说明：源代码管理面板里的「✨ 生成提交信息」**不一定**读取本 `.mdc` 文件；该按钮主要认仓库根目录的 **`.cursorrules`**。本规则仍约束 Agent / Chat 写提交说明。

在本工作区（含 `projects/` 下任意被管仓库）由 Cursor / Agent **生成或撰写提交信息**时，必须遵守：

1. **语言**：提交说明使用**中文**（type 与 scope 用英文 Conventional Commits 关键字）。
2. **格式**：Conventional Commits  
   - 示例：`feat(auth): 添加用户登录接口`  
   - 示例：`fix(ui): 修复按钮点击无效的问题`
3. **常用 type**：`feat`、`fix`、`docs`、`style`、`refactor`、`test`、`chore`。
4. **结构**：  
   - 第一行：概述（建议 ≤ 72 字，不以句号结尾）  
   - 第二行：空行  
   - 第三行起：必要时写详细说明（动机、影响范围；不要堆砌文件名列表）
5. **focus**：写清「为什么改」，而不是只罗列改了哪些文件。
6. **禁止**：纯英文整句 subject（如 `Implement ...` / `Add support for ...`）。

不要在各 `projects/<id>/` 内再复制一份仅为此用途的规则文件；优先依赖 LabHub 根目录 `.cursorrules` + 本规则。
